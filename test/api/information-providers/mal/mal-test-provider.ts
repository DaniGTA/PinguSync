import TestListProvider from '../../../controller/objects/testClass/testListProvider';

export default class MalTestProvider extends TestListProvider {
    constructor() {
        super('', true, true);
    }
}
