import TestProvider from '../../../controller/objects/testClass/testProvider';

export default class TraktTestProvider extends TestProvider {
    constructor() {
        super('', true, true);
    }
}
