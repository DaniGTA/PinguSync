export default class EpisodeBindingPoolReference {
    public id: string
    public index: number

    public constructor(id: string, index: number) {
        this.id = id
        this.index = index
    }
}
