export enum ProviderInfoStatus {
    /**
     * Has everything that the provider can give.
     */
    FULL_INFO,
    /**
     * Has episode number.
     */
    ADVANCED_BASIC_INFO,
    BASIC_INFO,
    ONLY_ID,
    NOT_AVAILABLE
}
