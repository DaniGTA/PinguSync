import MainListManager from '../main-list-manager/main-list-manager';

export default class CronUpdateSeriesList {
    getUpdateSeriesListCronJob() {
        const x = '';
    }

    private updateSeriesList() {
        const x = '';
    }
}