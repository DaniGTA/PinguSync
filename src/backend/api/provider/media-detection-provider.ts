import ExternalProvider from './external-provider';

/**
 * Provider MediaPlayer or Browser playback information to show the current status 
 */
export default abstract class MediaDetectionProvider extends ExternalProvider {
    version = 1;


}